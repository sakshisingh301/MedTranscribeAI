// ChatResponseBubble.js
import React from 'react';

const ChatResponseBubble = ({ message }) => {
  return (
    <div className="chat-response-bubble">
      {message}
    </div>
  );
};

export default ChatResponseBubble;
